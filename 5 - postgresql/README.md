# CRIANDO TABELAS
<code>CREATE TABLE clima(
    cidade VARCHAR(80),
    temp_min INT,
    temp_max INT,
    prcp REAL,
    dta DATE
);</code>

<code>CREATE TABLE cidades (
    nome VARCHAR(80),
    localizacao POINT
);</code>